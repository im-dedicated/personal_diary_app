# Personal Diary App

A simple diary application that lets users register, log in, create diary entries, edit them, delete them, and log out. All user and entry data is stored in JSON files under the data folder.

## Run locally

```bash
python app.py
```

Open http://127.0.0.1:8000 in your browser.

## Test

```bash
python -m unittest discover -s tests
```
